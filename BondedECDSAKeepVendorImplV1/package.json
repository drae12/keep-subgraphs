{
  "name": "keep",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ drae12/keep",
    "create-local": "graph create --node http://localhost:8020/ drae12/keep",
    "remove-local": "graph remove --node http://localhost:8020/ drae12/keep",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 drae12/keep"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
